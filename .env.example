# Copy to .env and adjust. Prisma loads this via dotenv in prisma.config.ts.

# Postgres — matches the docker-compose.yml db service (`npm run db:up`)
DATABASE_URL="postgresql://oja:oja@localhost:5432/oja_dev"
